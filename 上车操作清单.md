# 上车操作清单

适用：当前 ROS 2 Jazzy 项目，使用 `my_bt` 默认行为树的分步启动方式。

详细原理、代码位置、优化建议见 [导航系统说明与避坑指南](导航系统说明与避坑指南.md)。

**本清单是操作与验收顺序，不代表已完成实车验证。下面标出的配置缺口仍待处理。每一步正常后再进行下一步。**

## 0. 首次实车前先核对

- [ ] 转盘 `joint_up` 保持真实零位，模型中的零位与硬件一致。
- [ ] 雷达与 Odin 安装方向、位置符合 URDF。
- [ ] MID360 网络配置、底盘串口设备匹配实际硬件。
- [ ] 下位机能正确解释 x/y 速度（m/s）和转速（rad/s），方向一致。
- [ ] 已验证底盘通信断流会停止；目前上位机 mycontrol 未实现独立命令超时停机，需处理并核对下位机保护。
- [ ] 已处理 `controller_server` 里程计话题遗漏：当前默认订阅 `odom`，项目数据在 `/odin1/odometry`，或后续适配后的话题。
- [ ] 已核对 Odin 和其他传感器时间戳在同一时间轴，不能仅看话题有数据。
- [ ] 已核对里程计 twist 有效，且速度参考方向/参考点满足底盘控制需要。

## 1. 准备环境

每个终端执行：

```bash
source /opt/ros/jazzy/setup.bash
source /home/future/D/navigate/install/setup.bash
```

如果修改了 my_bt/myexplore 配置包，可在根目录构建：

```bash
cd /home/future/D/navigate
colcon build --symlink-install --packages-select myexplore my_bt
source install/setup.bash
```

硬件驱动、mycontrol 等包应已另外构建并安装依赖。目录名 `myBT`，ROS 包名 `my_bt`。

## 2. 分步启动

每条持续运行的命令放在独立终端，按表顺序执行并检查数据。

| 步骤 | 命令 | 先确认什么 |
| --- | --- | --- |
| ① 模型/显示 | `ros2 launch mysystem mysystem_rviz_launch.py` | 模型和部件 TF 正常 |
| ② 设备/串口 | `ros2 launch mycontrol mycontrol_launch.py` | 点云、Odin 数据、串口连接正常 |
| ③ 二维扫描 | `ros2 launch myexplore scan.launch.py` | `/scan` 能正确看到障碍物 |
| ④ 建图 | `ros2 launch myexplore slam.launch.py` | `/map` 正常、`map → base_link` 可查询 |
| ⑤ 导航 | `ros2 launch my_bt navigation.launch.py` | 导航节点 active、输入订阅正确 |
| ⑥ 自动探索 | `ros2 launch myexplore explore.launch.py` | 先通过单点导航验证，再启动此项 |

**采用这套流程时，不要再运行 `explore_bringup.launch.py`、原 `myexplore navigation.launch.py` 或重复的硬件驱动 launch。**

只建图时停在④，不启动⑤⑥。手动导航时启动到⑤，保持⑥关闭。

RViz 看地图时将 Fixed Frame 改成 `map`，添加 Map、LaserScan、TF 和路径等显示；当前保存的 RViz 配置主要用于看模型。

## 3. 发目标之前检查

以下数据/TF 查看命令请逐条运行，或放在不同终端；持续运行的命令用 Ctrl+C 结束后，再在同一终端执行下一条。

```bash
ros2 topic hz /scan
ros2 topic hz /odin1/odometry
```

频率检查会持续运行，用 Ctrl+C 结束观察。继续检查：

```bash
ros2 topic echo /odin1/odometry --once
ros2 run tf2_ros tf2_echo map base_link
```

结束 TF 观察后：

```bash
ros2 lifecycle get /bt_navigator
ros2 lifecycle get /controller_server
ros2 param get /bt_navigator default_nav_to_pose_bt_xml
ros2 param get /controller_server odom_topic
ros2 node info /controller_server
```

预期：节点为 active，默认树指向 my_bt，控制器订阅正确的里程计。确认消息类型和 QoS 相容；只存在同名话题还不够。

## 4. 测试一个手动目标

先在地图中挑选附近、已知可达的点，替换示例中的 x/y。此命令会发起真实导航任务。

```bash
ros2 action send_goal /navigate_to_pose nav2_msgs/action/NavigateToPose \
  "{pose: {header: {frame_id: map}, pose: {position: {x: 1.0, y: 0.0}, orientation: {w: 1.0}}}, behavior_tree: ''}" \
  --feedback
```

空的 `behavior_tree` 使用步骤⑤设置的默认树。请求被接受不等于完成，检查最终结果是否 `SUCCEEDED`。

取消导航要通过支持取消的 Action 客户端或相应界面操作并确认结果；不要把关闭命令行客户端当作已确认取消。

## 5. 车不动时按链路查看

```text
/cmd_vel_nav → /cmd_vel_smoothed → /cmd_vel → mycontrol → 串口 → 下位机
```

| 检查命令 | 用途 |
| --- | --- |
| `ros2 topic echo /cmd_vel_nav` | 控制器有没有产生速度 |
| `ros2 topic echo /cmd_vel_smoothed` | 平滑节点有没有输出 |
| `ros2 topic echo /cmd_vel` | 最终给底盘的速度是什么 |
| `ros2 topic echo /collision_monitor_state` | 是否触发停止或减速 |

最终速度为零时，先查上游原因。最终有非零速度但不动时，查串口和下位机。直接向 `/cmd_vel` 发命令会绕过上游平滑和碰撞监控，不要把它与完整导航链测试混为一谈。

## 6. 自动探索与手动切换

单点导航正常后才启动步骤⑥。查看状态：

```bash
ros2 topic echo /explore/status
```

暂停探索：

```bash
ros2 topic pub --once /explore/resume std_msgs/msg/Bool '{data: false}'
```

当前暂停逻辑会取消导航目标。等待取消完成、确认车停止后，再发手动目标。

恢复探索：

```bash
ros2 topic pub --once /explore/resume std_msgs/msg/Bool '{data: true}'
```

不要让自动探索与手动程序同时持续发目标。探索报告完成也要检查地图覆盖情况，不能只看状态文字。

## 7. 保存结果、结束运行

地图还在发布时保存：

```bash
mkdir -p /home/future/D/navigate/maps
ros2 run nav2_map_server map_saver_cli -f /home/future/D/navigate/maps/my_map
```

- [ ] 确认保存成功，目标文件名不会覆盖需要保留的地图。
- [ ] 停止探索并取消正在执行的导航，确认车已停止。
- [ ] 再关闭导航、SLAM、扫描、硬件和模型等终端。
- [ ] 记录现象、报错、修改过的参数；需要复现时保存关键话题的 rosbag。

二维地图保存不等于保存 SLAM 位姿图或 Odin 内部地图。下一次“加载地图导航”还需要配置定位流程，当前默认入口仍是建图模式。
